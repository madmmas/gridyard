{
  "name": "@gridyard/ui-kit",
  "private": true,
  "version": "0.0.0",
  "license": "Apache-2.0",
  "description": "Shared UI components: toolbar, dialogs, form fields, and the component registry for custom cell types.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "echo 'not implemented yet — see docs/06-mvp-scope-and-roadmap.md' && exit 0"
  }
}
