{
  "name": "web-demo",
  "private": true,
  "version": "0.0.0",
  "license": "Apache-2.0",
  "description": "Demo app wiring the Gridyard packages together.",
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "build:wasm": "wasm-pack build ../../crates/gridyard-wasm --target web --out-dir ../../apps/web-demo/public/pkg --dev",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@gridyard/grid-renderer": "*",
    "@gridyard/ui-kit": "*",
    "@gridyard/workspace-runtime": "*"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
