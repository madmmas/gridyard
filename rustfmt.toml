# Explicit rustfmt options so formatting doesn't silently shift if
# rustfmt's defaults change in a future release.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
use_try_shorthand = true
